#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!\n");
   // Added to test if my compiler has been set up correctly and if my commits update the GitHub code for everyone
   printf("Testing run and compile, Jordan\n");
   return 0;
}